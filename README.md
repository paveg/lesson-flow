# LessonFlow

**3分で始める、最も美しい予約システム**

オンラインレッスンの予約管理をシンプルに。手数料は業界最安クラス。

## 技術スタック

- **Frontend**: Next.js 14 (App Router), TypeScript, Tailwind CSS
- **UI Components**: shadcn/ui
- **State Management**: Zustand
- **Form Handling**: React Hook Form + Zod
- **Database**: Supabase (PostgreSQL) + Drizzle ORM
- **Authentication**: Supabase Auth
- **Payment**: Stripe Connect
- **Email**: Resend
- **Hosting**: Vercel

## セットアップ

### 必要要件

- Node.js 20以上
- pnpm 10以上
- Supabaseアカウント
- Stripeアカウント

### インストール

1. リポジトリをクローン

```bash
git clone https://github.com/yourusername/lesson-flow.git
cd lesson-flow
```

2. 依存関係をインストール

```bash
pnpm install
```

3. 環境変数を設定

```bash
cp .env.example .env.local
```

`.env.local`ファイルを編集して、必要な環境変数を設定してください。

4. データベースのマイグレーション

```bash
pnpm db:push
```

5. 開発サーバーを起動

```bash
pnpm dev
```

http://localhost:3000 でアプリケーションが起動します。

## スクリプト

- `pnpm dev` - 開発サーバーを起動
- `pnpm build` - プロダクションビルド
- `pnpm start` - プロダクションサーバーを起動
- `pnpm lint` - ESLintでコードをチェック
- `pnpm db:generate` - Drizzleのマイグレーションファイルを生成
- `pnpm db:push` - データベースにスキーマをプッシュ
- `pnpm db:studio` - Drizzle Studioを起動

## プロジェクト構造

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 認証が必要なページ
│   │   ├── dashboard/
│   │   └── onboarding/
│   ├── (public)/          # 公開ページ
│   │   ├── login/
│   │   └── page.tsx       # ランディング
│   ├── api/               # API Routes
│   ├── layout.tsx
│   └── globals.css
├── components/
│   ├── ui/                # shadcn/ui components
│   ├── forms/
│   ├── layouts/
│   └── features/
├── lib/
│   ├── db/
│   │   ├── schema.ts      # Drizzle schema
│   │   └── index.ts       # DB client
│   ├── stripe.ts
│   ├── supabase.ts
│   └── utils.ts
├── hooks/
├── types/
└── config/
```

## 機能

### MVP機能 (Phase 1)

- ✅ メールリンク認証
- ✅ Stripe Connect連携
- 🚧 レッスン作成（単発）
- 🚧 予約ページ生成
- 🚧 予約・決済処理
- 🚧 予約確認メール送信

### 実装しない機能 (Phase 1)

- ❌ パスワード認証
- ❌ 詳細なダッシュボード
- ❌ キャンセル・返金機能
- ❌ カレンダー連携
- ❌ 定期レッスン
- ❌ 複数講師対応

## デプロイ

### Vercelへのデプロイ

1. Vercelアカウントを作成
2. リポジトリを接続
3. 環境変数を設定
4. デプロイ

詳細は[Vercelのドキュメント](https://vercel.com/docs)を参照してください。

## ライセンス

MIT

## サポート

問題が発生した場合は、[Issue](https://github.com/yourusername/lesson-flow/issues)を作成してください。
